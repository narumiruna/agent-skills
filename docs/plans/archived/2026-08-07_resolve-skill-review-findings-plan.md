# Resolve Skill Review Findings Plan

## Goal

Resolve the six confirmed prompt and resource findings from the repository-wide active-skill review without changing unrelated skill triggers.

## Plan

- [x] Make bundled Marp and SVG examples render without missing local assets; verified all non-fenced local Markdown image paths under `skills/` resolve, six changed decks pass strict YAML and the bundled structural precheck, and no missing placeholder path remains. Marp CLI was unavailable for renderer inspection.
- [x] Make `applying-tdd` derive production and test layouts from repository evidence before conservative ecosystem fallbacks; verified the skill and boundary reference now prioritize manifests, package configuration, imports, established paths, and runner configuration.
- [x] Replace the UX option-count threshold with evidence-based task grouping and scanability rules; verified no seven/eight-option rule remains.
- [x] Make panel scores and thresholds optional signals while acceptance depends on verified surviving findings and checks; verified scores default off, no default threshold remains, and acceptance criteria use substantive reviews, surviving blockers, source verification, and checks.
- [x] Remove redundant or unrouted slide indexes, output examples, helper script, and palette example; verified every remaining routed resource exists and deleted paths have no references.
- [x] Consolidate Mermaid type guidance without duplicating basic `.mmd` examples; verified `references/types.md` contains only added decision guidance and all six routed basic examples pass structural header checks. Mermaid CLI was unavailable for renderer validation.
- [x] Run changed skill validators, relevant bundled script checks, repository link/resource audits, `prek run -a`, and `git diff --check`; verified 34/34 active skills, catalog alignment, routed resources, local links, Marp structure/YAML, Mermaid headers, all prek hooks, and a clean diff check.

## Completion Checklist

- [x] All six review findings are resolved with direct repository evidence.
- [x] Changed skills remain aligned with the README catalog and repository guidance.
- [x] Required validation passes with no unresolved local asset links or stale deleted-resource references.
